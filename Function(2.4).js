// Definition: Parameters are placeholders for the values that a function expects when it's called. They are defined in the function declaration.
// Location: Parameters are written inside the parentheses of the function definition.

Example:
function greet(name) {
  console.log("Hello, " + name + "!");
}
